# ========================================
# 하루한포 백엔드 환경 변수 템플릿
# ========================================
# 
# 이 파일을 복사하여 .dev.vars (로컬 개발용) 생성:
#   cp .env.template .dev.vars
#
# 주의: .dev.vars는 절대 Git에 커밋하지 마세요!
# ========================================

# ----------------------------------------
# Supabase 설정
# ----------------------------------------
# Supabase Dashboard에서 확인:
# Settings → API → Project URL
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co

# Settings → API → Project API keys → anon public
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Settings → API → Project API keys → service_role (주의: 절대 노출 금지!)
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ----------------------------------------
# Google OAuth 설정
# ----------------------------------------
# Google Cloud Console에서 확인:
# APIs & Services → Credentials → OAuth 2.0 Client IDs
GOOGLE_CLIENT_ID=123456789012-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com

# Client secret (주의: 절대 노출 금지!)
GOOGLE_CLIENT_SECRET=GOCSPX-abcdefghijklmnopqrstuvwxyz

# ----------------------------------------
# 토스페이먼츠 설정
# ----------------------------------------
# 토스페이먼츠 개발자센터에서 확인:
# https://developers.tosspayments.com/my/applications

# 클라이언트 키 (테스트용)
# 프로덕션: live_ck_... 형태로 교체
TOSS_CLIENT_KEY=test_ck_abcdefghijklmnopqrstuvwxyz1234567890

# 시크릿 키 (주의: 절대 노출 금지!)
# 프로덕션: live_sk_... 형태로 교체
TOSS_SECRET_KEY=test_sk_abcdefghijklmnopqrstuvwxyz1234567890

# ----------------------------------------
# OpenAI API 설정 (기존)
# ----------------------------------------
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-abcdefghijklmnopqrstuvwxyz1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ

# ----------------------------------------
# Google Gemini API 설정 (기존)
# ----------------------------------------
# https://makersuite.google.com/app/apikey
GEMINI_API_KEY=AIzaSyAbcDefGhIjKlMnOpQrStUvWxYz1234567

# ----------------------------------------
# 추가 설정 (선택사항)
# ----------------------------------------

# 환경 구분 (development | production)
ENVIRONMENT=development

# 디버그 모드 (true | false)
DEBUG=true

# 로그 레벨 (debug | info | warn | error)
LOG_LEVEL=debug

# 파일 업로드 최대 크기 (바이트 단위, 기본: 10MB)
MAX_FILE_SIZE=10485760

# 이미지 업로드 최대 개수 (기본: 5)
MAX_IMAGES=5

# 문서 업로드 최대 개수 (기본: 3)
MAX_DOCUMENTS=3

# ----------------------------------------
# Cloudflare Pages 배포 시 주의사항
# ----------------------------------------
#
# 1. .dev.vars는 로컬 개발 전용입니다
# 2. 프로덕션 환경 변수는 다음 방법으로 설정:
#
#    npx wrangler pages secret put VARIABLE_NAME --project-name haruhanpo
#
# 3. 각 환경 변수마다 위 명령을 반복 실행
# 4. 설정된 변수 확인:
#
#    npx wrangler pages secret list --project-name haruhanpo
#
# ----------------------------------------

# ========================================
# 보안 체크리스트
# ========================================
#
# [ ] .dev.vars 파일이 .gitignore에 포함되어 있는지 확인
# [ ] SUPABASE_SERVICE_KEY는 절대 프론트엔드에 노출 금지
# [ ] TOSS_SECRET_KEY는 절대 프론트엔드에 노출 금지
# [ ] GOOGLE_CLIENT_SECRET는 절대 프론트엔드에 노출 금지
# [ ] 프로덕션 배포 시 test_ 키를 live_ 키로 교체
# [ ] API 키는 정기적으로 교체 (최소 3개월마다)
#
# ========================================
